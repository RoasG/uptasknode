<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./mail.css
            
    body
        h2 Confirma tu cuenta UpTask
        p Ya estas a un paso, presiona el sgte boton
        a(href=`${confirmUrl}`) Confirma tu cuenta
        p Si no puedes abrir el enlace, visita: #{confirmUrl} 